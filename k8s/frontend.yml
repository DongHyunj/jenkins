apiVersion: apps/v1
kind: Deployment
metadata:
  name: board-frontend
  namespace: jdh
spec:
  replicas: 1
  selector:
    matchLabels:
      app: board-frontend
  template:
    metadata:
      labels:
        app: board-frontend
    spec:
      containers:
        - name: board-frontend
          image: deatytim/board-frontend:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 80
